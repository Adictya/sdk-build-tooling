export default class Test{
  public testName: string
  /**
   * @param param - A parameter that we accept
   * @returns string - the parameter that you passed
   */
  public testMethod(param:string):string{
    return param
  }
}
